/log/*
!/log/.keep
/tmp/*
!/tmp/.keep
!/tmp/pids/.keep
!/tmp/storage/.keep
/storage/*
!/storage/.keep

/.bundle
/vendor/bundle

# Rails secrets — never commit these
/config/master.key
/config/credentials/*.key

.env
.env.*
!.env.example

*.rbc

/public/assets
.byebug_history

bash.exe.stackdump

/config/credentials/development.key

# Local-only improvement notes (see IMPROVEMENTS.md) — not for the repo
IMPROVEMENTS.md
